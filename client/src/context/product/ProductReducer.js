import {
  CATEGORY_ADD,
  GET_CATEGORY,
  ADD_PRODUCT,
  ADD_VARIANT,
  EDIT_VARIANT,
  GET_PRODUCTS,
  CLEAR_ERRORS,
} from '../type';

export default (state, action) => {
  const { type, payload } = action;
  switch (type) {
    case value:
      break;

    default:
      return state;
  }
};
